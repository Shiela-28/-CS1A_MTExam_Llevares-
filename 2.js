//2: User Information Collection:

let subjectTitle = prompt("Enter the subject title:");
let classSchedule = prompt("Enter the class schedule (Time, Day):");
let classroom = prompt("Enter the classroom:");
let instructor = prompt("Enter the instructor's name:");

console.log{subjectTitle} is currently enrolled. 
The instructor for the subject is {instructor}. Scheduled at room ${classroom}.);